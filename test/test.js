const chai = require('chai');

describe('Database functionality', function() {
  describe('Users', function() {
    it('It should create a user on a post request to /users/create', function() {
      // expect(true).to.equal(true);
    });
  });
});
